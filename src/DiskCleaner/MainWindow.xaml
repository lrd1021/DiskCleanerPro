<Window x:Class="DiskCleaner.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="DiskCleaner Pro — C盘清理工具"
        Height="720" Width="1100"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}"
        MinHeight="600" MinWidth="900"
        shell:WindowChrome.WindowChrome="{StaticResource MainChrome}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="36" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="230" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- 自定义标题栏（暗色，与程序一致，消除启动白屏）-->
        <Grid Grid.Row="0" Grid.ColumnSpan="2" Background="{StaticResource BackgroundBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="14,0,0,0">
                <TextBlock Text="DiskCleaner Pro" Foreground="{StaticResource TextPrimaryBrush}" FontSize="13" FontWeight="SemiBold" />
                <TextBlock Text="— C盘清理工具" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="6,0,0,0" />
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Button x:Name="BtnMin" Click="BtnMin_Click" Width="44" Height="36"
                        shell:WindowChrome.IsHitTestVisibleInChrome="True" Style="{StaticResource TitleBarButton}"
                        FontFamily="Segoe UI" FontSize="14" Content="—" />
                <Button x:Name="BtnMax" Click="BtnMax_Click" Width="44" Height="36"
                        shell:WindowChrome.IsHitTestVisibleInChrome="True" Style="{StaticResource TitleBarButton}"
                        FontFamily="Segoe UI" FontSize="13" Content="▢" />
                <Button x:Name="BtnClose" Click="BtnClose_Click" Width="44" Height="36"
                        shell:WindowChrome.IsHitTestVisibleInChrome="True" Style="{StaticResource TitleBarCloseButton}"
                        FontFamily="Segoe UI" FontSize="13" Content="✕" />
            </StackPanel>
        </Grid>

        <!-- 左侧导航栏 — Grid 4 行布局 -->
        <Border Grid.Row="1" Grid.Column="0" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- Logo / 标题 -->
                <Border Grid.Row="0" Padding="24,32,24,24">
                    <StackPanel>
                        <TextBlock Text="DiskCleaner Pro" FontSize="20" FontWeight="SemiBold" Foreground="White" />
                        <TextBlock Text="C盘清理工具" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,0" />
                    </StackPanel>
                </Border>

                <Border Grid.Row="1" Height="1" Background="{StaticResource BorderBrush}" Margin="16,0" />

                <!-- 导航按钮列表 — 中间区域 -->
                <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Margin="0,8,0,0">
                    <ItemsControl ItemsSource="{Binding NavItems}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Content="{Binding}"
                                        Cursor="Hand" BorderThickness="0"
                                        FontSize="14"
                                        Margin="4,1"
                                        Command="{Binding DataContext.NavigateCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                        CommandParameter="{Binding}">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="bd" Background="Transparent" CornerRadius="8"
                                                    Padding="16,12" Margin="4,0">
                                                <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="bd" Property="Background" Value="{StaticResource CardHoverBrush}"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                                            <Style.Triggers>
                                                <DataTrigger Value="True">
                                                    <DataTrigger.Binding>
                                                        <MultiBinding Converter="{StaticResource NavEqualityConverter}"><Binding/><Binding Path="DataContext.SelectedNav" RelativeSource="{RelativeSource AncestorType=Window}"/></MultiBinding>
                                                    </DataTrigger.Binding>
                                                    <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
                                                    <Setter Property="Background" Value="#330078D4"/>
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="Button">
                                                                <Border Background="#330078D4" CornerRadius="8"
                                                                        Padding="16,12" Margin="4,0">
                                                                    <ContentPresenter Content="{TemplateBinding Content}"
                                                                      HorizontalAlignment="Left" VerticalAlignment="Center"
                                                                      TextElement.FontWeight="SemiBold"/>
                                                                </Border>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <!-- 设置按钮 — 固定在底部 -->
                <Button Grid.Row="3" Content="⚙️ AI 设置"
                        Style="{StaticResource GhostButton}"
                        HorizontalAlignment="Stretch" Margin="8,8,8,12"
                        Click="BtnSettings_Click" FontSize="13"
                        Foreground="{StaticResource TextSecondaryBrush}" />
            </Grid>
        </Border>

        <!-- 右侧内容区域 -->
        <Grid Grid.Row="1" Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <ContentControl Grid.Row="0" Content="{Binding CurrentPage}" />

            <Border Grid.Row="1" Background="{StaticResource SurfaceBrush}"
                    BorderBrush="{StaticResource BorderSubtleBrush}" BorderThickness="0,1,0,0"
                    Padding="16,8">
                <DockPanel>
                    <Ellipse Width="8" Height="8" Fill="{StaticResource SuccessBrush}" VerticalAlignment="Center" Margin="0,0,8,0" />
                    <TextBlock Text="{Binding StatusBarText}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" VerticalAlignment="Center" />
                </DockPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
